---
/**
 * /checkout — Checkout Page (Step 1)
 * KORA · NODO Studio
 */
import MainLayout           from "../../components/templates/MainLayout.astro";
import CheckoutForm         from "../../components/organisms/CheckoutForm.jsx";
import CheckoutProgressBar  from "../../components/molecules/CheckoutProgressBar.astro";
import { CheckoutContent }  from "../../data/checkoutContent.ts";
import { NavContent }       from "../../data/navContent.ts";

export const prerender = false;
---

<MainLayout
    title={`${CheckoutContent.steps.step1.label} — ${NavContent.logo}`}
    description="Completá tus datos para finalizar tu compra en KORA."
    noIndex={true}
>
    <CheckoutProgressBar step={1} />
    <CheckoutForm client:only="react" />
</MainLayout>
